---
import type { List } from 'datocms-structured-text-utils';

interface Props {
  node: List;
}

const { node } = Astro.props;
---

{
  node.style === 'numbered' ? (
    <ol>
      <slot />
    </ol>
  ) : (
    <ul>
      <slot />
    </ul>
  )
}
